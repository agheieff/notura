<div class="w-full">
  <div class="flex justify-between items-center mb-10">
    <h1 class="font-bold text-4xl">Welcome to Notura</h1>
    <div>
      <% if user_signed_in? %>
        <div class="flex items-center gap-4">
          <span>Welcome, <%= current_user.email %></span>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
        </div>
      <% else %>
        <div class="flex gap-4">
          <%= link_to "Sign in", new_user_session_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
          <%= link_to "Sign up", new_user_registration_path, class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded" %>
        </div>
      <% end %>
    </div>
  </div>
  
  <div class="bg-white p-8 rounded-lg shadow-md">
    <h2 class="text-2xl font-semibold mb-4">Your Personal Notura App</h2>
    <p class="mb-4">This application is set up with:</p>
    <ul class="list-disc pl-5 mb-4">
      <li>User authentication (sign up, sign in, sign out)</li>
      <li>PostgreSQL database</li>
      <li>Tailwind CSS for styling</li>
      <li>Hotwire (Turbo and Stimulus) for interactivity</li>
    </ul>
    <p>Ready to start building your application!</p>
  </div>
</div>
